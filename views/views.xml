<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="family.list">
      <field name="name">family list</field>
      <field name="model">family.family</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="family.action_window">
      <field name="name">family window</field>
      <field name="res_model">family.family</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="family.action_server">
      <field name="name">family server</field>
      <field name="model_id" ref="model_family_family"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="family" id="family.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="family.menu_1" parent="family.menu_root"/>
    <menuitem name="Menu 2" id="family.menu_2" parent="family.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="family.menu_1_list" parent="family.menu_1"
              action="family.action_window"/>
    <menuitem name="Server to list" id="family" parent="family.menu_2"
              action="family.action_server"/>
    -->
  </data>
</odoo>